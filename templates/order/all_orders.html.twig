{% extends 'base.html.twig' %}

{% block title %}Hello OrderController!{% endblock %}

{% block body %}
<div class="container">
    <p class="text-center mt-5">
        Votre commande a bien été pris en compte.
    <th>

    {# {% for user in users %}

    {% endfor %} #}
    <table>
        <thead>
            <tr>
                <th>Numéro de la commande</th>
                <th>first name</th>
                <th>Last Name</th>
                <th>created at</th>
                <th>Numéro de tel</th>
                <th>Adresse</th>
                <th>city</th>
                <th>total price</th>
            </tr>
        </thead>
    <tbody>

        {% for order in orders %}
            <tr>
                <td>Numéro de la commande</td>
                <td>{{order.firstName}}</td>
                <td>{{order.lastName}}</td>
                <td>order.createdAt</td>
                <td>{{order.phone}}</td>
                <td>{{order.adress}}</td>
                <td>order.city</td>
                <td>{{order.totalPrice}}</td>
            </tr>    
        {% endfor %}

    </tbody>
    </table>
    {% for order in orders %}
    {# <p>{{product.order.name}}</p> PAS BON #}
        {# <p>{{product.product.name}}</p> #}
        {# <p>{{order.product.name}}</p>
        <p>{{order.product.price}}</p>
        <p>{{order.product.images}}</p>
        <p>{{order.product.name}}</p>
        <p>{{order.product.name}}</p> #}
    {% endfor %} 


</div>

{% endblock %}
